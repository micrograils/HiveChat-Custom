# HiveChat-Custom

```bash
git clone https://github.com/HiveChat/HiveChat-Custom.git
cd HiveChat-Custom
git submodule update --init --recursive
```

```bash
git add .
git commit -m "Update"
git push
```
